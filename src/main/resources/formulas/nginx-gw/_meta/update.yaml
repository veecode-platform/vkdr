# Update configuration for automated dependency updates
type: helm-pinned
helm:
  repo: oci://ghcr.io/nginx/charts/nginx-gateway-fabric
  chart: nginx-gateway-fabric
  version: "2.6.7"
  version_file: install/formula.sh
  values_file: null
# Gateway API CRDs are pinned separately in install/formula.sh (GWAPI_CRDS_YAML),
# shared with kong-gw - bump it together with the chart
test: make test-formula formula=nginx-gw
