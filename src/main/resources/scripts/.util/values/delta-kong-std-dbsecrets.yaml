env:
  database: postgres
  pg_user: kong
  pg_host: vkdr-pg-cluster-rw
  pg_password:
    valueFrom:
      secretKeyRef:
        key: password
        name: vkdr-pg-cluster-role-kong
  pg_database: kong
postgresql:
  enabled: false
  auth:
